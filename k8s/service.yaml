apiVersion: v1
kind: Service
metadata:
  name: personal-service
  labels:
    app: personal-service
spec:
  type: ClusterIP
  selector:
    app: personal-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
